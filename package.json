{
  "name": "tranche-lp",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run --filter @tranche-lp/web dev",
    "build": "bun run --filter @tranche-lp/web build",
    "start": "bun run --filter @tranche-lp/web start",
    "lint": "bun run --filter @tranche-lp/web lint",
    "format": "bun run --filter @tranche-lp/web format"
  }
}
